#!/usr/bin/env node
const path = require("path");
const fs = require("fs");

const localPath = path.join(__dirname, "../dist/index.js");
fs.exists(localPath, (exists) => {
  if (exists) {
    require(localPath)();
  } else {
    require("episodic-cli")();
  }
});
